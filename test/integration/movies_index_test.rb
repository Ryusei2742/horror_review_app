require "test_helper"

class MoviesIndexTest < ActionDispatch::IntegrationTest
  # test "index including pagination" do
    # get movies_path
    # assert_template 'movies/index'
    # assert_select 'div.pagination'
    # Movie.paginate(page: 1).each do |movie|
      # assert_select 'a[href=?]', movie_path(movie), text: movie.title
    # end
  # end
end
